import { Grid } from '@chakra-ui/react';
import styled from 'styled-components';

const StyledVStack = styled(Grid)`
  @media screen and (max-width: 1060px) {
    margin-top: 1em;
  }
`;

export default StyledVStack;
