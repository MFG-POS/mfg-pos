const SaveBoard = (tables: {
  [key: string]: { top: number; left: number; text: string; width: number; height: number; borderRadius: number };
}) => {
  console.log(tables);
};

export default SaveBoard;
